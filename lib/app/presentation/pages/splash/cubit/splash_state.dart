part of 'splash_cubit.dart';

/// The code snippet is defining an enumeration called `SplashState` in Dart. An enumeration is a special data type that
/// represents a set of named values. In this case, `SplashState` has three possible values: `idle`, `loggedIn`, and
/// `noUserExist`.
enum SplashState {
  idle,
  loggedIn,
  noUserExist,
}
